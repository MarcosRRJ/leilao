/*angular.module('app.providers')
	.factory("tokenInterceptor", appCliente);

appCliente.$inject = ['$rootScope', '$location'];

function appCliente($rootScope, $location) {

	return {

		'request': function(config){

			config.headers.Authorization = 'Bearer ' +  vm.storageStorage.token;

			return config;
		}
	};

};*/